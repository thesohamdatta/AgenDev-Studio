version: '3.8'

services:
  autodev-enterprise:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./workspace:/app/workspace
      - ./memory:/app/memory
    environment:
      - STREAMLIT_THEME_BASE=dark
    restart: always
